import { FounderOnboarding } from "@/components/onboarding/founder/FounderOnboarding";

export const metadata = {
  title: "Founder onboarding · Kori",
};

export default function Page() {
  return <FounderOnboarding />;
}
