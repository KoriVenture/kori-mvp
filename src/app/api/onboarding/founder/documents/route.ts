import { NextResponse } from "next/server";
import { documentUploadSchema } from "@/lib/onboarding/contracts";
import { requireRole } from "@/lib/onboarding/http";
export async function POST(request:Request){const a=await requireRole("founder");if(a.error)return a.error;const form=await request.formData();const file=form.get("file");if(!(file instanceof File))return NextResponse.json({error:"A file is required."},{status:400});const check=documentUploadSchema.safeParse({documentType:form.get("documentType"),title:form.get("title"),size:file.size,mimeType:file.type});if(!check.success)return NextResponse.json({error:"Unsupported document."},{status:400});const startupId=String(form.get("startupId")??"");const owned=await a.supabase.from("startups").select("id").eq("id",startupId).eq("founder_user_id",a.userId).maybeSingle();if(!owned.data)return NextResponse.json({error:"Startup not found."},{status:404});const safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,"-");const path=`${a.userId}/${startupId}/${crypto.randomUUID()}-${safeName}`;const upload=await a.supabase.storage.from("startup-data-room").upload(path,file,{contentType:file.type,upsert:false});if(upload.error)return NextResponse.json({error:"Upload failed."},{status:500});const saved=await a.supabase.from("startup_documents").insert({startup_id:startupId,uploaded_by_user_id:a.userId,document_type:check.data.documentType,title:check.data.title,storage_path:path});if(saved.error){await a.supabase.storage.from("startup-data-room").remove([path]);return NextResponse.json({error:"Document could not be recorded."},{status:500});}return NextResponse.json({ok:true,path},{status:201});}
