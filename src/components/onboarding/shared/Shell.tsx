"use client";import type{ReactNode}from"react";import{Editorial}from"./Editorial";
export function Shell({step,total,title,subtitle,children,next,back,save,founder=false,action="Continue"}:{step:number;total:number;title:string;subtitle?:string;children:ReactNode;next:()=>void;back?:()=>void;save:()=>void;founder?:boolean;action?:string}){return <main className="account-page"><Editorial founder={founder}/><section className="form-panel"><header className="form-header"><span>Step {step+1} of {total}</span><button className="text-button" onClick={save}>Save and exit</button></header><form className="account-form long-form" onSubmit={e=>{e.preventDefault();next()}}><div className="step-progress"><span>STEP {String(step+1).padStart(2,"0")} OF {String(total).padStart(2,"0")}</span><i style={{width:`${step/Math.max(total-1,1)*100}%`}}/></div><div className="intro"><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div>{children}<div className="form-actions">{back&&<button type="button" className="secondary-button" onClick={back}>Back</button>}<button className="primary-button" type="submit">{action}</button></div></form><footer>Secure demonstration platform · No real funds</footer></section></main>}
