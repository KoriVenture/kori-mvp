export function SelectField({label,name,options,onChange}:{label:string;name:string;options:string[];onChange?:(value:string)=>void}){return <label>{label}<span className="select-wrap"><select name={name} required defaultValue="" onChange={e=>onChange?.(e.target.value)}><option value="" disabled>Select</option>{options.map(x=><option key={x}>{x}</option>)}</select><img src="/assets/onboarding/shared/chevron-down.svg" alt=""/></span></label>}
